// This is a legacy header file - use CompactHeader instead
// Kept for backwards compatibility, but redirects to CompactHeader

import { CompactHeader } from "./CompactHeader";

export const Header = () => {
  return <CompactHeader />;
};